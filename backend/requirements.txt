# Web server
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.13.4
python-dotenv==1.2.2

# Agent orchestration
langgraph==1.2.4

# LLM providers (all-Gemini: generator + judge)
langchain-google-vertexai==3.2.3    # via Vertex AI (ADC)
langchain-google-genai==4.2.4       # via AI Studio API key
google-cloud-aiplatform==1.154.0

# Data + storage
supabase==2.31.0                    # image_assets / runs / questions tables
boto3==1.43.26                      # S3 run-JSON upload + image storage backend
Pillow==12.2.0
google-genai                        # gemini-2.5-flash-image (generate_images.py worker)

# Optional — only if you switch a role to another provider in the admin panel:
# langchain-anthropic==1.4.4
# anthropic==0.109.1
# langchain-openai

# Audio worker (Google Cloud TTS — the bird voice)
google-cloud-texttospeech
